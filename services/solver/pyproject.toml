[project]
name = "timesflex-solver"
version = "0.1.0"
description = "Constraint-based timetable solver for TimeFlex"
requires-python = ">=3.11"
dependencies = [
	"fastapi>=0.115.0",
	"uvicorn>=0.30.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
